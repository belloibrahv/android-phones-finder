export interface Phone {
  id: string;
  name: string;
  brand: string;
  price: number;
  primaryCamera: string;
  features: string[];
  batteryLife: string;
  screenSize: string;
  imageUrl: string;
  isNew: boolean;
  dimensions: string;
  storage: string[];
  ram: string[];
  screenResolution: string;
  releaseYear: number;
}
